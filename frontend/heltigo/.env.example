# ═══════════════════════════════════════════════════════════════
# Heltigo Frontend — Environment Configuration
# ═══════════════════════════════════════════════════════════════
# Copy this file to `.env` and adjust values for your environment.
# `.env` is loaded at app startup via flutter_dotenv.
# ═══════════════════════════════════════════════════════════════

# Backend base URL (full URL including /api/v1)
#
# Pilihan umum:
#   1) ngrok (testing dari device fisik):
#      https://your-tunnel.ngrok-free.dev/api/v1
#
#   2) Emulator Android (host machine localhost):
#      http://10.0.2.2:3000/api/v1
#
#   3) iOS Simulator / Web / Desktop:
#      http://localhost:3000/api/v1
#
API_BASE_URL=https://cheryll-unintelligent-fuzzily.ngrok-free.dev/api/v1
